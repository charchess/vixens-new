# kubernetes/apps/metallb/base/kustomization.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metallb-system
resources:
  - namespace.yaml
  - metallb.yaml            # CRD + RBAC + Deploy + DaemonSet officiel
  - ipaddresspools.yaml
  - l2advertisement.yaml