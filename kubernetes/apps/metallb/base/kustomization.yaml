# kubernetes/apps/metallb/base/kustomization.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metallb-system

resources:
  # 1. namespace
  - namespace.yaml
  # 2. manifest upstream complet (CRD + RBAC + Deployment + DaemonSet)
  - https://github.com/metallb/metallb/raw/v0.15.2/config/manifests/metallb-native.yaml
  # 3. nos custom resources
  - ipaddresspools.yaml
  - l2advertisement.yaml