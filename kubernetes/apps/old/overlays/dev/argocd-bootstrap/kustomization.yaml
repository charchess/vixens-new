# overlays/dev/argocd-bootstrap/kustomization.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - namespace.yaml
  - argocd-project.yaml
  - argocd-root-app.yaml

# install.yaml a été retiré car il est géré par le script bootstrap.

  