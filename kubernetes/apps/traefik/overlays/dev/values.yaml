service:
  type: LoadBalancer
  annotations:
    metallb.universe.tf/loadBalancerIPs: "192.168.208.170"
    metallb.universe.tf/address-pool: service-pool-cluster

ports:
  web:
    expose: true
    redirectTo: ""
  websecure:
    expose: true
    tls:
      enabled: false

# dashboard via IngressRoute CRD (déjà dans base/)